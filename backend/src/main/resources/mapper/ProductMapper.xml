<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.arthaschan.metagptmall.mapper.ProductMapper">

  <resultMap id="ProductMap" type="com.arthaschan.metagptmall.entity.Product">
    <id property="id" column="id"/>
    <result property="title" column="title"/>
    <result property="description" column="description"/>
    <result property="priceCents" column="price_cents"/>
    <result property="currency" column="currency"/>
    <result property="stock" column="stock"/>
    <result property="imageUrl" column="image_url"/>
    <result property="active" column="active"/>
    <result property="createdAt" column="created_at"/>
  </resultMap>

  <select id="findActiveProducts" resultMap="ProductMap">
    SELECT id, title, description, price_cents, currency, stock, image_url, active, created_at
    FROM products
    WHERE active = 1
    ORDER BY id
    LIMIT #{size} OFFSET #{offset}
  </select>

  <select id="countActiveProducts" resultType="long">
    SELECT COUNT(*) FROM products WHERE active = 1
  </select>

  <select id="findById" resultMap="ProductMap">
    SELECT id, title, description, price_cents, currency, stock, image_url, active, created_at
    FROM products
    WHERE id = #{id}
  </select>

  <!-- Optimistic stock deduction: fails if stock insufficient -->
  <update id="decreaseStock">
    UPDATE products
    SET stock = stock - #{quantity}
    WHERE id = #{id}
      AND stock >= #{quantity}
  </update>

</mapper>
